{% extends 'base.html.twig' %}

{% block content %}
    <h1>Portfolios</h1>
    {% block page_contents %}

        <table class="table table-hover">
            <thead class="thead-light">
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Name</th>
                <th scope="col">Value</th>
            </tr>
            </thead>
            <tbody>
            {% for portfolio in portfolios %}
                <tr>
                    <th scope="row">{{ portfolio.id }}</th>
                    <td><a href="{{ path('portfolio_details', {id: portfolio.id}) }}" >{{ portfolio.name }}</a></td>
                    <td>{{ portfolio.value }} {{ portfolio.valueCurrency }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    {% endblock %}
{% endblock %}
